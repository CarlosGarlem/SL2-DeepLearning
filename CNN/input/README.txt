Note: 
For the problem resolution take into account the test, train and val(validation) files provided. 
This files take into account the "one_indexed_files" which will reflect the class they belong from 1 to 6

*Ignore the zero_indexed_file since we are using the one_indexed_file notation